extends layout

block content
	.container(style="text-align:center;")
		.row
			.col-md-4.col-md-offset-4
				p Yo #{user.username}!
				ul
					li #{user._id}
					li #{user.salt}
					li #{user.hash}
					li #{user.username}
					li #{user.activated}
					li #{user.actCode}
					li #{user.rank}
					li #{user._v}
				span #{test}